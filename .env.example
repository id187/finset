# Leave blank for local Vite proxy or public fixture replay.
# Set only after deploying an API that supports /api/v2/meta and /api/v2/recommend,
# with HTTPS and an explicit CORS allowlist for the Pages origin.
VITE_FINSET_API_URL=
