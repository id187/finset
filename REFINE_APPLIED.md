# refine 적용 · 2026-09-15

[질문형 추천](https://id187.github.io/finset/?screen=guided) · [모바일 캡처](docs/refine/refine-result-412.png) · [PC 캡처](docs/refine/refine-result-1280.png) · [목표 부족 화면](docs/refine/refine-goal-gap.png)

기준은 `mvp/refine.zip/핀셋_MVP담당_시각화와추천흐름_갱신본`의 수정명세서·질문지 검토본·화면배치 참고다. 전달된 감사 스크립트는 이 저장소에 없는 긴급 수정 어댑터를 전제로 하므로 그대로 복사하지 않았다. 별도 정책 `refine-2026-09-15-v1`을 적용하고 감사의 전체열거 방식을 현재 실행 코드에 맞춰 다시 검증했다. 원본 코어 버전은 `2026-09-13-consistency-1`이다.

## 변경한 행동

- 기본 5단계 뒤 필요한 가입 정보와 공통 자동이체만 확인한다. 납입 방식에 따라 후보가 달라질 때만 정액·자유적립 선택을 추가한다.
- 자동이체 거절·미확인은 해당 우대 없이 추천을 완료한다. 같은 의사를 은행별로 반복해 묻지 않는다. 상품별 계약기간으로 인정 개월 수를 계산하며 계좌 출처·가입 시 설정·연장은 추정하지 않는다.
- 가입 미확인 후보는 따로 보류하고 가입 확인 후보를 비교한다. 미확인 우대는 원본 조건을 거짓으로 기록하지 않고, 계산용 규칙 사본에서만 제외한다. 미확인 우대 상한으로 기본 순위를 올리거나 전체 추천을 잠정 상태로 만들지 않는다.
- 현재 미보유는 보유 수 0으로 재사용한다. 합산 월 납입 한도에는 이번 납입액을 포함하며 과거 6개월 미보유까지 추정하지 않는다.
- 카드 사용·신규 카드 실적은 제외한다. 추가 거래 거절과 확인된 기존 거래·등급·금액·온라인 가입 혜택을 구분한다. 행동 비용은 확인 전 반영하지 않는다. 금액·등급·온라인 가입 자체에는 새로운 지출 행동이 없다.
- 추가 혜택은 결과에서 사용자가 고른 항목만 확인한다. 거절하면 그 분기를 끝내며 비용은 해당 행동을 선택한 경우에만 묻는다. 금액·기간을 편집하면 진행 중이던 선택적 질문을 닫는다. 개인 사실과 공통 자동이체 답변은 유지한다.

## 화면

- 목표명·기간과 예상 목표금액 막대, 대표 상품 1개, 펼칠 수 있는 대안 최대 2개를 제공한다. 은행명·기본금리·실제 적용 우대·세후 이자·필수 행동을 구분한다.
- 막대는 `min(예상/목표, 1)`이며 초과분을 별도로 표시한다. 0 또는 미확인 목표에는 막대를 만들지 않는다. 실제 납입 달성률이나 완주 확률로 표시하지 않는다.
- 같은 목표·예산 기준에서만 대안의 금액 차이를 정확한 원 단위로 표시한다. 큰 요약 금액의 정확한 값과 약관 원문은 상세에 있다.
- 1억원·5년·월 30만원의 원래 목표를 유지하며 원금 1,800만원과 원금 기준 필요한 월 1,666,667원을 안내한다. ‘지금 여력으로 상품 보기’는 목표를 변경하지 않는다.
- 첫 상품 만기와 최종 목표일을 분리한다. 만기 이후 재가입 이자는 원본 코어와 같이 0원이다.
- 기본 단계 숫자와 완료 막대를 구분한다. 동적 추가 질문에는 임의의 남은 비율을 표시하지 않는다. 답변 이력은 접혀 있고 이전 답을 수정할 수 있다.
- 전달본에 실제 들어 있던 공식 아이콘 2개를 원형 그대로 사용한다. 나머지는 약칭이며 실패한 이미지도 약칭으로 바뀐다. 360px에서 합성한 긴 상품명의 줄바꿈도 확인했다. 출처는 `public/banks/sources.json`에 기록했다.

## 실행 검증

| 검증 | 결과 |
|---|---|
| JS 회귀·표시·답변 상태 | 49개 통과 |
| 네이티브 Python 회귀·정책 | 기존 26개 + 새 정책 15개 통과 |
| WebAssembly 테스트 | 3개 통과: 전체 응답·자유 입력·별도 DB 쓰기/손상 거절 |
| 새 정책 네이티브 ↔ 실제 브라우저 Worker | 전체 응답 해시 280개 + 자유 입력·오류 14개 일치 |
| 독립 후보 조합 전체열거 | 6·12·18·60개월 × 목돈/적금/혼합 × 납입 방식 2종 × 자동이체 예/아니요, 48개 순위 일치 |
| 실제 UI 클릭 | 기본 질문, 예/아니요/모름, 선택적 혜택 거절, 금액 수정, 목돈만, 변동 수입, 목표 부족, 첫 만기 확인 |
| 화면 너비 | 360·390·412·430·768·1280px 가로 넘침 없음, 결과의 버튼·상세 열기 높이 44px 이상 |
| 입력·보존 | 연속 입력 포커스 유지, 이전 값 유지, 기존 localStorage 변경 없음 |
| 실행 경로 | API 요청 없이 같은 사이트 GET만 사용; 초기 준비 후 오프라인 계산 일치 |
| 배포 빌드 | TypeScript 및 `build:pages` 통과 |

[UI 실행 기록](docs/refine/refine-ui-audit.json) · [Worker 대조 기록](docs/refine/browser-core-audit.json) · [48개 순위 상세](tests/fixtures/refine-ranking.json) · [응답 해시 280개](tests/fixtures/refine-responses.json) · [실제 추천·이유 JSON 14개](tests/fixtures/browser-core.json)

고정 입력(2026-09-15 시작, 1년, 월 30만원, 초기 0원, 목표 360만원, 은행 범위)의 대표 결과:

| 자동이체 | 추천 | 적용 연 금리 | 목표일까지 예상 |
|---|---|---:|---:|
| 직접 납입 | 코드K 자유적금 | 3.70% | 3,661,181원 |
| 매달 만기까지 가능 | 카카오뱅크 자유적금 | 3.85% | 3,663,514원 |
| 모르겠음 | 코드K 자유적금 | 3.70% | 3,661,181원 |

거절과 모르겠음은 서로 다른 우대 상태로 남는다. 목표·납입액·조건이 달라지면 결과도 달라진다. 순위 전체열거는 원본 `candidate`의 가입·이자 계산을 재사용하고 조합과 정렬만 독립 대조했다. 모든 현실 사용자에게 최선임을 증명한 검사는 아니다.

## 보존과 남은 한계

원본 DB, `source_lock.json`, vendor 파일·규칙은 수정하지 않았다. 전체 원본 DB SHA256은 적용 전후 `15597888b0965ef565783957abea501d38130c0585ba3c86cd6355d86b954ef7`로 같다. 상품 12,542개의 별도 목록 DB는 1,703,936바이트, 새 정책을 포함한 Python 압축 패키지는 2,819,607바이트다. 이는 전체 DB 약관의 의미 검증을 새로 완료했다는 뜻이 아니다.

은행의 급여 인정 문구처럼 전달 자료만으로 객관적 판정 규칙을 확정할 수 없는 조건은 직업·입금 주체만으로 인정 처리하지 않는다. 검토된 금액·기간·계좌 질문만 연결하고, 여전히 미확인인 우대는 금액에 더하지 않는다. 현재 정책은 최대 우대 가능성을 모두 조사한 순위가 아닌 **확인한 조건 기준 추천**이다.

키보드는 실제 휴대전화가 아니라 브라우저의 `visualViewport` 축소 이벤트로 메뉴 숨김을 검증했다. 물리 기기의 키보드 동작은 별도 확인이 필요하다. 상품 조건은 2026년 8월 수집 스냅샷이며 실시간 판매·가입·실적 확인 기능은 추가하지 않았다. 회복·완주의 기존 메뉴와 데이터는 유지했다.

재현: `python -X utf8 -m unittest discover -s tests -p 'test_*.py'`, `python -X utf8 scripts/export-refine-validation.py`, `npm run test:model`, `npm run test:pages`, `npm run test:browser-core`, `npm run build:pages`, `node scripts/audit-refine.cjs`, `node scripts/audit-browser-core.cjs`. 네이티브 검사는 기존 전달 DB가 필요하며 Pages 빌드·Worker 검사는 커밋된 검증 패키지만으로 실행한다.
