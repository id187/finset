# 핀셋 목업 검수

검수일: 2026-09-13. 대상은 사용자가 요청한 **웹·앱 크기의 인터랙티브 목업, 기존 실제 데이터 연결, 원본을 보존한 별도 조건 DB와 질문 판정**입니다. 전달된 MVP 명세서는 화면 흐름과 데이터 표시 원칙의 준거로 사용했습니다. 운영용 FastAPI·인증·서버 세션 저장까지 완성한 MVP라는 주장은 하지 않습니다.

## 요청별 근거

질문형 시연은 기획서와 최신 요청에 따라 **목표·월 저축액 → 자동이체 질문 → 추천** 2단계로 간소화했습니다. 월 금액 직접 입력과 +1만·+3만·+5만 반복 증액을 지원합니다. 금융정보는 화면에서 명시한 가상 인물 기준이며 자동이체 답변으로 금리·추천 순위가 달라집니다. 원본 DB와 상품 연결 범위를 유지하고 새 시연의 저장 절차는 제거했습니다.

`tests/guided.test.mjs`는 원래 Python 엔진의 645개 기준 결과 및 계산기 출력 110개를 비교합니다. `scripts/audit-guided.cjs`는 2단계, 임의 금액·증액/감액, 목표 수정, 자동이체 미확인·추천 변경, 부족액 대안, 한도 제외, 새로고침 초기화, 기존 저장 데이터 보존, 웹·앱 화면과 미리보기 크기를 검수합니다. 실행 결과는 `.qa/guided-results.json`입니다.

| 요구 | 현재 결과 | 확인 근거 |
|---|---|---|
| GitHub 저장소 연결 | `origin`은 `https://github.com/id187/finset.git` | `main` push 시 `.github/workflows/pages.yml`로 배포 |
| GitHub Pages | `/finset/` 공개 목업과 `/finset/preview.html` 크기별 미리보기 | 기존 엔진의 1,000개 시연 결과, 원본 DB 업로드 제외, API 없이 브라우저 동작 |
| 웹 1280×800 / 앱 412×917 | 내부 뷰포트가 정확히 해당 크기인 미리보기 | `audit-mockup.cjs`에서 iframe의 `innerWidth`, `innerHeight` 직접 비교 |
| 작은 모바일 대응 | 360×800에서도 페이지 가로 넘침 없음 | `check-final.cjs`: 7개 화면 × 3개 크기 검사. 월별 표는 컴포넌트 내부 스크롤 허용 |
| 실제 상품 데이터 활용 | 2026년 8월 수집 스냅샷 + 기존 Python 코어 사용 | 실제 API 결과로 추천 카드 렌더·저장. 기본 가상 사례의 코드K 자유적금 표시 확인 |
| 원본 보존 | 원본 DB와 기존 규칙을 읽기 전용 참조 | DB SHA256 `15597888b0965ef565783957abea501d38130c0585ba3c86cd6355d86b954ef7` 일치, query_only와 integrity_check 통과 |
| 별도 조건 DB | `app_data/bonus_conditions_v1.sqlite3` 생성 | 원문 12,542개, 기존 규칙 25,560개, 원문 조각 17,578개. `conditions-report.json`과 실제 SQLite 확인 |
| 문장 → 개별 질문 → 적용 판정 | 카카오·토스 2개 상품 9개 기간 옵션 연결 | 미확인 제외, 개월 수 경계, 자동연장 원리금 제외, 만기 계획 조건, 우대 %p 합산 테스트 |
| 불명확한 조건 처리 | 원문 검수가 필요한 조건은 자동 적용 차단 | 기존 우대 규칙 35개 옵션은 검수 상태. 변형 원문·알 수 없는 응답 키·잘못된 타입 거부 |

## 화면과 동작

| 화면 흐름 | 검수한 동작 |
|---|---|
| S01 시작하기 | 계획 시작 / 저장한 계획 / 가상 대시보드 진입 |
| S02 목표·여력·비교 조건 | 3단계 이동, 입력 보존, 잘못된 금액 안내 및 필드 포커스, 실제 추천 호출 |
| S03 추가 확인 | 미확인 사례를 실제 엔진으로 계산, 요청 질문 표시, 질문 판정 화면 이동, 미확정 초안 저장 |
| S04 추천·대안 | 최대 3안, 엔진 순서 유지, 변경된 입력의 이전 결과 선택 차단, 연결 오류·재시도 |
| S05 저장 | 선택 확인 후 새 계획 저장, 최초 원금 0원, 초안은 현재 계획 유지, 새로고침 복원 |
| S06 대시보드 | 기록 원금·목표 잔여 원금·진행률·다음 납입·상품 표시, 저장한 계획 조회 |
| S07 납입 | 실제 날짜와 예정 회차 연결, 미래 날짜 거부, 일부 납입, 수정·취소, 정정 이력 보존 |
| S08~09 회복 | 일시/지속/긴급 구분, 원금 비교, 명시적 확인 전 적용 불가, 늦은 응답 무시 |
| S10 변경 이력 | 변경 전후 일정 버전 보존, 원금·기존 납입 유지, 계약 조건 미확인 배지 유지 |

일시 감액 월 20만원: 최종 원금 350만원. 남은 8회 지속 감액 월 20만원: 최종 원금 280만원. 두 경우 기존 원금 120만원 유지. 긴급 필요 50만원·별도 현금 20만원: 부족 30만원, 인출·해지 손실 미확인, 자동 적용 버튼 없음.

60개월 목표의 회복에서도 상품 만기를 변경하지 않으며, 만기 이후 별도 저축 일정의 금액을 유지하는 것을 실제 화면과 저장 데이터로 확인했습니다.

## 실행 결과와 산출물

- `npm run build -- --logLevel warn`: 타입 검사 및 프로덕션 빌드 성공.
- `node --experimental-strip-types --test tests/model.test.mjs`: 7개 테스트 통과.
- `npm run test:pages`: 1,000개 스냅샷 입력·해시 확인, Python 우대 225개·회복 37개 기준값 일치, 잘못된 응답 차단 통과.
- `npm run build:pages`: GitHub Pages용 타입 검사·정적 빌드.
- `scripts/audit-pages.cjs`: API 없는 정적 배포의 7개 화면·3개 크기와 상품 저장·초안·기록 수정·취소·회복·우대 질문·크기별 미리보기. 결과는 `.qa/pages-results.json`.
- `python -X utf8 -m unittest discover -s tests -v`: 14개 테스트 통과.
- `scripts/audit-mockup.cjs`: 실제 API 연결을 사용하는 전체 주요 흐름 10개 묶음 통과. 장애 복구 화면만 명시적으로 503을 주입해 검수.
- `scripts/check-final.cjs`: 7개 화면·3개 크기, 대화상자 이름/Escape/포커스 복원, 오래된 회복 응답 차단, 긴 목표와 만기 구분 통과.
- `.qa/audit-results.json`, `.qa/final-results.json`: 실제 실행 결과.
- `.qa/final-*.png`: 최종 화면별 스크린샷.
- `.qa/finset-walkthrough.webm`: 자동 동작 검증 영상. 사람이 설명하는 2~3분 발표 영상은 아님.

## 목업의 명시적 범위

새 질문형 시연의 답변은 메모리에만 유지하고 저장하지 않습니다. 별도 완성 예시의 납입·계획 저장 기능은 기존대로 유지합니다. 가상 금융정보와 사용자가 입력한 금액·자동이체 답변을 구분하며, 모든 상품의 우대 문장이 검수된 것은 아닙니다. 카카오·토스 외 우대 질문 확대는 별도 데이터 검수가 필요합니다. 코드K 포함 3개 상품의 제한된 비교 범위와 기존 API 원본 엔진·규칙을 유지합니다.

실계좌·이체·가입 실행, 운영용 사용자 인증과 세션 격리, 서버 측 사용자 DB, 모든 입력 항목과 금융권을 다루는 전체 MVP, 실제 계약 변경 승인·확정 이자·중도해지 손실 계산은 이 목업의 완성 주장에 포함하지 않습니다. 전달 명세서의 전체 T01~T35 운영 MVP 인수 통과와 위 목업 검수를 혼동하지 않습니다.
