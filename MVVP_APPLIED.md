# mvvp 적용 기록

적용일: 2026-09-14. 기준 폴더: `mvp/mvvp/핀셋_MVP담당_전달자료_갱신버전`.

## 확인한 차이

mvp2와 비교했을 때 Python 코어와 기획서 v1.5는 동일합니다. mvvp에는 `질문과_결과화면_구현기준.md`와 HTML이 추가됐고, 첫 안내·개발 적용 순서에서 이 기준을 연결했습니다. 이번 수정은 mvvp의 기준을 적용합니다.

## 적용

- 코어 버전 `2026-09-13-consistency-1`. 원본 파일의 manifest 해시를 검증하며 수정하지 않음.
- `engine.recommend`, `questions.render` 직접 호출. 기존 회복 서버와 격리한 Python 프로세스 사용.
- 은행·저축은행·신협 범위, 객관적 사실, 실천 의향, 납입 방식의 구분 유지.
- 선택만으로 넘어가지 않는 이전·다음 버튼, 선택 체크 표시, 키보드 조작.
- 목표·월 금액 프리셋 교체, 원 단위 입력·천 단위 구분·0원 처리·입력 유지.
- 질문 본문 최대 640px, 본문 16px, 터치 영역 44px 기준.
- 목표일과 상품 만기 분리, 목표 부족 안내 우선 표시.
- 내 계획의 각 항목에서 입력한 값이 있는 해당 단계로 수정 이동.
- 관련 조건이 바뀐 우대·기간 답만 재확인, 관련 없는 답 보존.
- `compare`는 최종 카드 없이 비교안 표시. `null`은 잠정 상태 유지.
- 반환한 `goal_fit`, `goal_notice`, `role`, `comparison_explanation`을 이용해 추천과 최대 2개 대안 표시.
- 금리보다 예상 금액 강조, 세금·납입 전제와 필요한 우대 행동을 펼치기 밖에 표시.
- 예금·적금 조합의 구성별 금액 표시. 짧은 목표에 코어가 반환한 입출금 비교도 별도로 표시.
- 중복 제출·오래된 응답 방지, 오류 재시도 시 입력 유지.
- 기존 회복·계획·저장 경로와 네 회 납입 예시 유지.
- 발표자료 15장 구성을 유지하며 실제 수정 화면과 현재 시연 범위를 반영. 약 72초 시연 녹화 생성.

## 검증

| 확인 | 결과 |
|---|---|
| 전달 fixture 14개 | 전체 Python 응답 일치 |
| 코어 도움말 | 6개 모두 존재 |
| 코어 상태 전환 | 전달 단위 테스트 통과, 두 초기 상태 검사 |
| 전송 어댑터 | 7개 Python 테스트 통과 |
| JS 회귀와 새 상태·리플레이 | 40개 테스트 통과 |
| 실제 자유 입력 | 월 371,259원 그대로 전달, 질문 후 COMPARISON 결과 |
| 날짜 유지 | 60개월 목표를 36개월로 자르지 않음 |
| 공개 시연 | 대표·우대 거절·미정·목표 부족·후보 없음·납입 방식 비교 확인 |
| 화면 | 1280×800, 412×917, 360×800에서 가로 넘침 없음 |
| 오류·기록 | 같은 요청 재시도, 키보드 선택, 기존 네 회 납입 기록 보존 |
| 원본 보존 | 원본 DB·rules·liquid_rules·v1 조건 DB 해시 일치, DB 무결성 통과 |

검수 기록: `.qa/mvvp-local-audit.json`, `.qa/mvvp-pages-audit.json`, `.qa/mvvp-edge-audit.json`, `.qa/mvp2-core-verification.json`, `.qa/mvvp-preserved-data.json`. 이는 실제 사용자 이해도나 저축 성과의 검증을 뜻하지 않습니다.

## 실행·확인

로컬 실행과 데이터 준비는 README.md를 따릅니다. `/api/v2/meta`, `/api/v2/recommend`가 새 코어 경로입니다. GitHub Pages는 준비된 Python 응답을 재생하며 임의 입력을 JS로 다시 계산하지 않습니다. 전체 원본 DB와 전체 추천 규칙은 배포하지 않습니다.

기본 시연: 우대 행동 미정 사례 → 거절 → 코드K 결과 → 우대 선택 수정 → 동의 → 카카오뱅크 결과. 기간 36개월·방식 미선택 사례에서는 차이 보기 → 정액 또는 자유적립 선택을 확인합니다. 목표금액 부족 사례에서는 부족액 안내가 추천 카드보다 먼저 나옵니다.

공유 작업 폴더의 `output/FinSet_기획설명_v4_mvvp_최종.pptx`, `output/FinSet_mvvp_시연.webm`이 발표·녹화 파일입니다. 실제 화면 캡처는 `finset/.qa/ppt-*-v4.png`, `mvvp-*.png`에 있습니다.

## 남은 범위

- 공개 자유 입력 계산: 추천 API 서버가 아직 없어 Pages에서는 준비된 사례만 계산 결과를 제공합니다. 서버 배포 후 `VITE_FINSET_API_URL`로 연결해야 합니다.
- 현재 판매 여부·금리·가입 승인·실제 우대 실적: 2026년 8월 수집 스냅샷으로는 확인하지 않습니다.
- 전체 약관 의미 검수: 기존 전체 구조 검사와 구분합니다. 미해석 약관·추가 조건 검수는 별도 작업입니다.
- 회복 Figma 발표 화면: 접근 가능한 Figma 파일·링크가 전달되지 않아 임의 화면을 만들지 않았습니다. 기존 회복 대시보드와 PPT의 기획 예시를 유지했습니다.
- 실제 계좌·가입·이체, 복수 사용자 저장, 실제 사용자 테스트: 운영 단계의 작업입니다. 새 질문 흐름에 저장 절차를 추가하지 않았습니다.
