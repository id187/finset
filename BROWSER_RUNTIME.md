# 서버 없는 질문형 추천 · 2026-09-15

GitHub Pages의 기본 5단계, 추가 질문, 답변 수정, 목표 부족 안내, 최종 추천을 브라우저에서 직접 계산한다. 별도 API 서버나 고정 응답 선택은 필요하지 않다.

## 실행과 자료

- Pyodide **314.0.7**을 npm lockfile로 고정하고 같은 Pages 사이트에서 제공한다. 외부 CDN이나 API에 입력을 보내지 않는다.
- `core.worker.ts`가 별도 Worker에서 Python을 실행한다. 화면 입력을 막지 않고 기본 질문을 작성하는 동안 초기 준비를 시작한다.
- 순위·가입 조건·우대 의향·이자·질문 생성은 원본 Python 코어를 사용한다. `core_vendor/manifest.json`에 고정한 모든 파일과 규칙은 변경하지 않는다.
- `browser_entry.py`는 기존 `core_runtime.execute()`를 호출한다. 실행 전에 `source` 모듈의 데이터 접근 부분만 `browser_source.py`에 연결한다.
- 현재 `engine.py`와 `liquid.py`는 `source.connect()`로 자료를 검증하며, 계산에는 원본 규칙을 사용한다. 원본 DB의 수집 페이지 본문을 계산할 때마다 읽지 않는다.
- 원본 DB **131,260,416바이트**는 수정하거나 배포하지 않는다. 원본 전체 SHA256을 검증한 뒤 상품 **12,542개**의 공개 목록 6개 열을 별도 SQLite에 복사한다. 추출 결과가 원본 조회 결과와 일치하는지도 검사한다.
- 별도 DB **1,703,936바이트**는 `mode=ro&immutable=1`, `PRAGMA query_only=ON`으로만 연다. 파일 자체의 SHA256을 별도로 확인한다.
- 원본 규칙·실행 코드·상품 원문 표시 자료·작은 DB를 포함한 압축 패키지는 약 **2.8MB**다. Python 실행 파일은 별도로 약 **13.5MB**이며 브라우저 HTTP 캐시를 사용한다.
- `source_sha256`은 **패키지 생성 시 전체 검증한 원본의 출처 해시**다. 브라우저가 원본 125MB 파일을 다시 검증했다는 의미가 아니다. 브라우저는 배포 패키지와 별도 DB의 해시를 검증한다.
- 추천 입력과 응답은 현재 화면과 Worker 메모리에만 존재한다. 기존 가상 납입 기록의 저장 방식은 유지한다.

## 실패 처리

로딩 중 안내를 표시하고, 파일 누락·해시 불일치·Worker 오류·120초 초과 시 재시도할 수 있다. 요청마다 식별자를 사용하고 취소한 요청의 응답은 반영하지 않는다. Python 호출은 순차 처리한다. 계산 실패를 준비된 다른 사람의 사례로 대체하지 않는다.

## 검증

- 이전 14개 사례의 답변 조합 **280개 전체 응답**이 브라우저 실행과 일치.
- 사례에 없는 월 37만원, 목돈 527만원, 목돈·적금 조합, 18개월·5년, 변동 수입, 단기 보관, 미확인·거절, 납입 방식 비교, 0원과 잘못된 입력 등 **13개**를 별도 CPython 기대값과 대조.
- 초기 준비 후 네트워크를 끊어도 자유 입력 계산 일치. 모든 초기 요청은 같은 사이트의 GET이며 `/api/` 요청 없음.
- 412×917 기본 질문 → 추가 질문 → 최종 추천, 이전 답 유지, 금액 수정, 월 0원 예금 추천 확인. 1280×800, 360×800에서도 가로 넘침 없음.
- 로컬 Edge 테스트: 입력 화면 약 0.6초, 초기 계산 준비 약 5.5초, 이후 요청 평균 약 0.12초. 실제 네트워크와 기기에 따라 달라진다.
- 기존 JS 테스트 45개, Python 전송 테스트 7개와 가용 금액 테스트 3개 통과. 새 WebAssembly 검증 3개는 전체 응답 일치와 별도 DB의 쓰기·손상 거절을 확인하며 GitHub Actions에서도 실행한다.
- 패키지 손상 시 계산 중단, 같은 요청으로 재시도 성공, 초기 로딩 중 취소한 추천 응답 미반영을 실제 브라우저에서 확인.
- 최종 정적 배포 파일은 약 **36.2MB**로, Python 실행 파일과 기존 화면·상품 자료를 모두 포함한다.

## 갱신

원본 패키지가 있는 개발 환경에서 `python -X utf8 scripts/export-browser-core.py`로 별도 패키지를 생성한다. 기존 DB와 규칙을 재생성하지 않는다. 패키지·메타데이터·출처 manifest를 함께 커밋한다. `npm run build:pages`는 원본 DB 없이도 동작하며, 실행 코드·출처·버전·패키지 해시가 서로 다르면 빌드가 실패한다.

상품 조건은 기존 **2026년 8월 수집 자료**이며 실시간 공시나 실제 가입·이체 기능을 추가한 것은 아니다.

실행 방식 참고: [Pyodide Worker](https://pyodide.org/en/stable/usage/webworker.html), [정적 사이트 배포](https://pyodide.org/en/stable/usage/downloading-and-deploying.html).
